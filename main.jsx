import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './app.jsx' // Make sure your app file is named exactly app.jsx (all lowercase)

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)

